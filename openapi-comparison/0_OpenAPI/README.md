# Cách generate OpenAPI spec từ Flask app
- requrirements: `pip install flask-restx pyyaml`
- Run `python gen.py` 

# Cách chạy server và sử dụng swagger UI
- Run `python main.py`
- Access `http://localhost:5000/swagger` sau đó thao tác trên giao diện để test API