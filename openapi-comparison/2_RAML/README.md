RAML (RESTful API Modeling Language) = YAML-based spec (giống OpenAPI nhưng thiên về design-first)

Triết lý chính:

- Thiết kế API trước → rồi mới code
- Vì vậy RAML không có thư viện chính thức nào để tự động generate spec từ Flask app. 
- Hoặc generate ra từ OpenAPI spec (như trong 0_OpenAPI).